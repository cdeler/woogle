version: '2'
services:
  downloading:
    build: .
    command: ["python","app.py",'-o','db','--loglevel','INFO','--concurrency','2','--host','0.0.0.0']
    networks:
      network:
        aliases:
          - downloading.network_elastic_backend

networks:
  network:
    external:
      name: network_elastic_backend


